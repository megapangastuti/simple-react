1. harus install node js
2. vite bisa digunakan secara online, nggak perlu install node js dulu
3. sekarang node js nya sebaiknya di atas 20 versinya
4. swc itu adalah build tools berbasis rust => menggantikan bible => run time doang
5. cdn itu tidak efektif untuk menjalankan react js, karena fitur2 js harus dibikin sendiri dan harus dipanggil
6. vite punya online code editor => jadi g harus install node js
   => bisa buka disini https://vite.new/react-ts
   => bisa login, bisa disave projectnya dan bisa diconnect dengan github kita
7. di react => unopinianated => serah luu => bisa ngatur structure sendiri => bisa tambah library serah lu =>
8. constants => simpen nilai yang nggak berubah2
   => contohnya env => isinya token
9. routes => mengelola routing
10. service => isinya file yang berisi fungsi2 buat hit api
11. style => styling
12. types => file yang digunakan buat simpen file types spesifik
13. utils => menyimpan file utilitas  
    => convert currency
14. react dibangun di atas function js
15. functional component => component yang dibangun di atas fungsi
16. jsx adalah function yang mengembalikan syntax html
17. komponen => memungkinkan kita untuk membagi UI menjadi beberapa bagian yang independen dan dapat digunakan kembali (reusable)
18. best practice :
    - satu file hanya berisi satu component
    - komponen ditulis menggunakan js function dengan return HTML (JSX)
    - penamaan component baik fungsi maupun nama file harus menggunakan Pascal case untuk membedakan dengan HTML tag
    - satu komponen harus mengembalikan sebuah element HTML
19. <> </> => tag kosong
20. folder layouts
    - page login dan resgister kan mirip => makanya pake layouts => jadi bisa reuse aja
    - sesuatu yang sama yang bisa dipakai di beberapa pages yang berbeda
21. commons => beberapa hal yang sering terpakai
22. minimal ada folder
    layout, pages, sama ui
23. barrel import?
24. nested component
    - aplikasi biasanya
25. props => untuk mengirimkan data yang bisa kita tangkap langsung lewat komponen
26. props children =>
27. conditional rendering
    - bekerja dengan cara yang sama seperti kondisi bekerja di JS
    - gunakan operator js seperti
28. rendering list

    - merupakan hal yang umum untuk memiliki data yang banyak yang perlu ditampilkan sebagai list
    - menggunakan function
    -

29. beberapa template eslint yang dipake => eslint rbnb atau eslint google
30. vite.config.ts => untuk configurasi vite sebagai runtime
